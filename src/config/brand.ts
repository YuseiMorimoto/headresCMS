/**
 * Astro と Node スクリプトの両方から参照する表示文言の定義元。
 *
 * `site.ts` は `import.meta.env` を参照するため tsx 実行のスクリプトから import できない。
 * 検証スクリプトと画面表示で文言がずれると、表記を変えた瞬間に
 * verify-content が「PR表記が見つかりません」でデプロイを止めてしまうため、
 * `import.meta` を参照しないモジュールとして分離している。
 */

/** 景表法・ステマ規制対応の広告表記。全記事に必ず表示する（docs/02） */
export const PR_NOTICE = "※本記事にはアフィリエイトリンクを含みます";

/** OGP / meta description 用のサイト説明 */
export const SITE_DESCRIPTION =
  "問いの場は、答えを急がず、問いを起点に人生を深く考えるための知のプラットフォームです。";

/** 運営者情報ページなどで使うサイトの目的説明 */
export const SITE_MISSION =
  "当サイトは、問いを手がかりに自分自身と向き合い、人生を深く考えるための場をつくることを目的としています。記事を通じて新しい視点に出会い、あなた自身の問いを見つける手助けをします。";
